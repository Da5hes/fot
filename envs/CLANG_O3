export CC=clang
export CXX=clang++
export CFLAGS="-g -O3"
export CXXFLAGS="-g -O3" 
unset LDFLAGS

SCRIPTPATH="$( cd "$( dirname "${BASH_SOURCE[0]}" )" >/dev/null 2>&1 && pwd )"
source $SCRIPTPATH/FUZZING

export CC=clang
export CXX=clang++
export CFLAGS="-g -O0"
export CXXFLAGS="-g -O0" 
unset LDFLAGS

SCRIPTPATH="$( cd "$( dirname "${BASH_SOURCE[0]}" )" >/dev/null 2>&1 && pwd )"
source $SCRIPTPATH/FUZZING
